{
  "name": "signal-intake-hub",
  "private": true,
  "version": "0.2.0",
  "description": "Public framework for ingesting signals and generating Discord analysis threads",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run --workspaces --if-present build",
    "test": "npm run --workspaces --if-present test"
  }
}
